# CycleMind 答辩口播稿（外行也听得懂版）

> **怎么用这份稿子：**
> - 每页有三块。**【这页念这个】** 照着念就行，已经是口语，不用临场组织；**【一句话过渡】** 翻到下一页时顺口说出来，让故事不断线；**【被追问 / 想秀深度】** 是给评委追问时垫底的，外行模式直接跳过，想下沉技术再用。
> - 全场正文不出现"裸术语"。凡是技术词，后面一定跟一句"也就是……"的人话翻译。
> - 整场正文约 **8–12 分钟**。时间紧，就只念下面这段「30 秒电梯版」。

---

## 🛗 30 秒电梯版（时间紧就只念这段）

> "我们做的东西叫 **CycleMind**。一句话说清楚：**你用大白话说一句『我想做个什么软件』，它就像装修公司帮你出全套设计图一样，自动帮你把开工前要准备的五样东西——一份需求清单、一张系统结构图、一张数据库关系图、一份接口说明书、一份开发计划——全部画好、写好。**
>
> 而且它不是填表，是**跟你聊天**把想法问清楚；聊清楚之后，**好几个 AI 专家同时开工**，谁先做完你就先看到谁的成果；你再补一句『加个支付功能』，它在原来的基础上改，而且**每改一版都存档、旧版不删**，等于给你的设计文档配了一台时光机。"

---

## 📒 术语对照速查表（被连环追问时查这张）

| 幻灯片上的术语 | 一句话大白话翻译 |
|---|---|
| Sub-Agent（子 Agent） | 一个只干一件事的 AI 专家，比如专门写需求的、专门画数据库图的 |
| Orchestrator（编排器） | "调度员"。我们**没单独设这个角色**，把派活的活儿合并进了接待员自己 |
| tool_call / function calling | 让 AI 自己决定"现在该动手了"并直接按按钮，而不是我们写死规则去猜它的意思 |
| `trigger_generation` | 那个"开始生成"的按钮，由 AI 自己判断时机来按 |
| SSE（流式推送） | 像看快递物流：进度一步步实时弹给你，而不是干等到全做完才一次性给 |
| `Promise.all()` 并行 | 几个专家**同时**开工，而不是排队一个做完再做下一个 |
| append-only 版本 | 只新增、不覆盖。每改一版存一份新的，老版本永远留着 |
| `parentVersionId` 版本链 | 每一版都记着"我是从哪一版改来的"，能一路追溯到最初 |
| Drizzle ORM / Neon PostgreSQL | 我们用的数据库和它的操作工具，都是业界主流 |
| 数据库索引（B-tree） | 给数据建"目录页"，查东西从一页页翻变成直接翻到那一页，快得多 |
| Mermaid | 一个能把文字描述自动画成流程图/关系图的工具 |
| 动态 import / `ssr:false` | 一种"用到才加载"的省流量技巧，让页面打开更快 |
| Zustand / Store | 管理界面状态的工具，相当于一个"前台公告板"，各处共享同一份信息 |
| agentic vs workflow | "放权 vs 流水线"。聊需求时让 AI 自由发挥，生成文档时走固定流水线，各用各的长处 |
| Human-in-the-loop | "人来把关"。AI 想动手前先弹个确认框，你点头才真干 |
| Web Vitals / Analytics | 自动统计网页快不快、卡不卡的体检工具，不用我们手动埋点 |

---

## 封面 —— CycleMind / AI 驱动的软件文档生成系统

**【这页念这个】**
"各位老师好，我们这个项目叫 CycleMind。先别看屏幕上这些技术名词，我用一句生活里的话给大家讲清楚我们做了个什么东西——
你有没有想过，真正要开发一个软件之前，程序员其实不能上来就敲代码，得先准备一大摞'图纸和说明书'：要做哪些功能、系统长什么样、数据怎么存、各部分怎么对接、分几步开发完。这些东西又费时、又枯燥、还得是懂行的人才会写。
**我们做的，就是让你只用一句大白话说出想法，AI 自动把这一整摞图纸和说明书全给你生成出来。**"

**【一句话过渡】**
"那为什么这件事值得做？我们先看看现在大家是怎么受这份罪的。"

---

## 01 —— 问题与定位

**【这页念这个】**
"现在的痛点是这样：开发前的这些设计文档，第一是**全靠手写，特别慢**；第二是**互相脱节**——需求改了，结构图忘了改，数据库图又对不上，几份文档各说各的；第三是**一改就过时**，今天写完明天需求一变，文档立马作废，没人愿意回去维护。
我们的思路就一句话：**你跟它聊天说清楚需求，它自动判断什么时候该动手，一次把整套文档生成出来；而且你每改一次，它都存一个新版本，老版本不删。** 这样文档第一次真正'活'了起来——不再是写完就扔在一边的死文件，而是跟着你的想法一起长大的东西。"

**【一句话过渡】**
"那这套东西底下是用什么搭起来的？我快速带过一下，这页不用细看。"

---

## 02 —— 技术栈总览

**【这页念这个】**
"这页全是我们用的工具，我一句话带过：**我们用的都是当下业界最主流、最现代的一套全栈工具**，从前端界面、数据库，到 AI 的大脑，全是行业里最新的标准配置。具体每一样是什么，写在屏幕上了，老师感兴趣我可以展开讲。"

**【被追问 / 想秀深度】**
"前端是 Next.js 16 + React 19 + TypeScript；数据库是 Neon 云端 PostgreSQL，配 Drizzle 这个操作工具；登录用 Auth.js；AI 用的是 DeepSeek 大模型，通过标准接口调用；部署在 Vercel 上，**全程零运维**，不用我们自己管服务器。"

**【一句话过渡】**
"工具讲完，看看这些工具是怎么串成一条流水线的。"

---

## 03 —— 整体核心架构

**【这页念这个】**
"这页是我们整个系统的骨架，我用一个比喻讲——**把它想象成一个 AI 项目团队**：
你在左边跟一个'接待员'聊天；接待员觉得需求聊清楚了，会弹一个确认框问你'我准备帮你生成这几样东西，行吗？'；你一点头，**后台五个专家就同时开工**；每个专家做完一份文档就立刻存档、并实时显示在右边；最后你在一个三栏的工作台里，左边继续聊、右边看成果。
**说白了就是：聊天 → 确认 → 五个专家并行干活 → 实时出文档。**"

**【被追问 / 想秀深度】**
"接待员判断时机是靠大模型的 tool_call，也就是让 AI 自己决定该不该动手；五个专家用 `Promise.all()` 真并行；每份文档存的时候带版本号和'父版本'指针，形成一条可追溯的演化链。"

**【一句话过渡】**
"先说这五个专家产出的东西最后存在哪——也就是我们的数据怎么组织的。"

---

## 04 —— 数据模型（五张核心表）

**【这页念这个】**
"这页是讲数据怎么存的，对外行老师我只强调**一个最有意思的设计**：我们的文档表是**'只增不改'**的——你每生成或修改一次，它不是把旧的盖掉，而是**新存一份，并且记下它是从哪一版改来的**。所以你随时能翻回任何一个历史版本，看清楚这套设计是怎么一步步演化过来的。就像文档的'时光机'。其它几张表就是存用户、存会话、存聊天记录的，常规设计。"

**【被追问 / 想秀深度】**
"五张表：users / sessions / documents / messages / templates。documents 永不覆盖，version 自增、parentVersionId 指向上一版。所有外键都建了索引，所以会话、文档、消息的查询都是 O(log n)，不会随数据变多而变慢。"

**【一句话过渡】**
"数据讲完，回到最有意思的部分——那几个 AI 专家到底各干什么。"

---

## 05 —— AI Agent 系统总览

**【这页念这个】**
"这是我们系统的核心。我把它翻译成**一个 AI 团队：一个接待员，加五个专家**——
**接待员**，负责跟你聊天、把需求问清楚，再决定派哪几个专家上场；
然后五个专家，各管一摊，**一人只干一件事，干得特别专**：
一个是**产品经理**，把你的大白话整理成正式的需求清单；
一个是**架构师**，画系统结构图；
一个是**数据库设计师**，画数据关系图；
一个是**接口工程师**，写各部分怎么对接的说明书；
一个是**项目经理**，排开发计划和工期。
关键是——**接待员不一定每次都把五个人全叫上**，你只说'帮我设计数据库'，它就只叫数据库设计师，叫谁干活它自己判断。"

**【被追问 / 想秀深度】**
"每个 Agent 本质是一段精心设计的 Prompt，配独立的温度和 token 上限，职责彻底解耦。要特别说明：**我们没有单独的 orchestrator agent**，编排决策被收进了对话 Agent 自己——这正是我们这次重构的核心，后面会展开。"

**【一句话过渡】**
"那这个'接待员'到底是怎么一边聊天一边判断'该动手了'的？这是我负责的核心部分，我重点讲。"

---

## 06 —— 对话 Agent（接待员怎么工作）

**【这页念这个】**
"先说这个接待员有意思在哪。它**不会一上来就乱生成**——你刚说一句'我想做个图书管理系统'，它不会立刻甩给你一堆文档，而是**像个有经验的顾问一样反过来问你**：'这东西给谁用？要有哪些核心功能？打算用什么技术？'，一点点把需求聊清楚。
而且它回你话是**一个字一个字蹦出来的**，跟你平时用 AI 聊天那种打字效果一样，不会让你盯着空白屏幕干等。
等它觉得'需求够清楚了'，它心里就有数：该动手了。"

**【被追问 / 想秀深度】**
"它走的是流式 SSE，实时返回；身上只挂了一个工具叫 trigger_generation，模式是 tool_choice auto——也就是由模型自己判断要不要调用它来触发生成。"

**【一句话过渡】**
"而'它怎么判断该动手、又怎么真的动手'这件事，是我们这次最大的一处重构，我特别想讲讲。"

---

## 07 —— 核心重构（最重点的一页）

**【这页念这个】**
"这页是我们整个项目我最自豪的改动。我用大白话讲清楚我们改了什么——
**老办法**是：让接待员在它的回话里偷偷塞一个暗号，比如说一句话里藏个'READY'标记；后台再用程序去文字里**找这个暗号**；找到了，再去叫一个专门的'调度员'来安排干活。问题是——这个暗号**特别容易认错**，AI 偶尔在别的地方提一句就误触发了，整个流程很脆。
**新办法**是：我们干脆给接待员一个**正经的'开始生成'按钮**，让它**自己直接去按**，按的时候顺手把'该叫哪几个专家、为什么'一起填好。这样**又准、又能查到它当时为什么这么决定、还省掉了那个多余的调度员**。
说白了，就是**从'让 AI 打暗号、我们去猜'，升级成'让 AI 自己按按钮、明明白白告诉我们'。**"

**【被追问 / 想秀深度】**
"老方案是 `[READY]` 文本魔法标记 + 后端正则匹配 + 再调一次独立 orchestrator，两次模型调用、标记易误触发、解析脆弱。新方案是标准 function calling：直接调 trigger_generation，参数自带 agents[]、summary、reasoning；后端把流式 delta.tool_calls 分片累积起来再一次性 JSON.parse。单次调用、决策可审计、彻底取消了 orchestrator。"

**【一句话过渡】**
"按钮一按，五个专家就同时开工了——我们来看这个'同时'是怎么做到的。"

---

## 08 —— 多 Agent 并行编排

**【这页念这个】**
"这页讲'同时开工'。你看这几条进度条——五个专家**不是排队**一个做完再做下一个，而是**一起动手**。谁先做完，谁的成果就先弹出来给你看，不用等最慢的那个。
打个比方就像装修：水电工、设计师、预算员**同时进场各干各的**，而不是一个走了下一个才来。所以**总时间等于最慢那个专家的时间，而不是五个人加起来**——速度差好几倍。
而且整个过程像看快递物流：'某专家开始了''某专家出成果了''某份文档存好了'，一步步实时弹给你。"

**【被追问 / 想秀深度】**
"用 Promise.all() 同时点火五个 Agent，谁先完成谁先 createDocument 并通过 SSE 推 doc_saved；事件流是 agent_start → agent_output → doc_saved，最后 done 汇总。并行墙钟 = 最慢的 Agent，而非五者之和。"

**【一句话过渡】**
"那如果不是第一次生成，而是来改的呢？专家怎么知道之前做过什么？"

---

## 09 —— 跨 Agent 上下文（迭代式生成）

**【这页念这个】**
"这页讲'改'。当你回来补一句'再加个支付功能'，专家**不会推倒重来重画一遍**，而是**先把上一版翻出来当参考，在原来的基础上接着改**。就像装修改方案，设计师是在你原来那张图上动笔，而不是撕了重画。
而且每改一次都存一版，v1、v2、v3 一路记着，你随时能回看这套设计是怎么长起来的。"

**【被追问 / 想秀深度】**
"生成前先 getLatestDocument 取上一版作参考，演化而非重画，版本链一路追溯。"

**【一句话过渡】**
"讲完我们怎么做的，也对比一下我们这套和业界常见的 AI 架构有什么不一样。"

---

## 10 —— 与传统 Agent 架构的区别

**【这页念这个】**
"这页稍微学术一点，我用一句话总结：业界做 AI 系统通常走两个极端——**要么全放权**，让 AI 自己反复折腾（容易乱、容易空转烧钱）；**要么全写死**，按固定剧本走（不够聪明）。
我们用的是**混合**：**该放权的地方放权，该确定的地方确定**。聊需求这种灵活的事，让 AI 自由发挥；生成文档这种该稳的事，走固定的流水线。两样的长处都占着。"

**【被追问 / 想秀深度】**
"对话层是 agentic（LLM 自主决定何时、生成什么），生成层是 workflow（确定性并行 fan-out），呼应 Anthropic 那篇《Workflow vs Agent》。对比 ReAct 自主循环和传统 Orchestrator+Workers，我们是两者的混合。"

**【一句话过渡】**
"当然这套设计有得也有失，我们也认真想过它的短板。"

---

## 11 —— 优点与缺点

**【这页念这个】**
"我们不回避缺点。**好处**是：花钱可控不空转、速度快、分工清楚、动手前有人把关、每个决策都能查到原因。
**短板**也有：专家是'一锤子买卖'，做完不会自己回头检查对不对；五个人同时干，偶尔几份文档之间会对不齐；长对话聊久了可能忘掉最早说的细节。这些我们都想好了改进方向，比如给专家加一道'自我复查'的工序。
我觉得**能清楚说出自己的缺点，恰恰说明我们是真的把这套系统想透了。**"

**【被追问 / 想秀深度】**
"缺点对应方案：子 Agent 无自纠错 → 加 critique/verify 回路；并行文档可能不一致 → 一致性校验；上下文粗粒度 → 引入 RAG；任务图固定 → 不通用但够用；长会话丢早期上下文 → 摘要压缩。"

**【一句话过渡】**
"以上是我负责的核心部分。下面交给我的两位队友，讲讲他们各自啃下来的硬骨头。先是负责界面这块的增宇。"

---

## 12–16 —— 团队协作 · 任增宇（前端）

> 这五页是同一个人的连续内容，可整段讲，也可按页停。核心一句话：**让这东西好看、好用、过程让人安心。**

**【这页念这个】**
"大家好，我负责的是用户**真正看到、摸到的那一面**，目标就一句话：**让它用起来顺手、看着舒服、生成的时候让人放心。** 具体做了几件事——
**第一，那个三栏工作台**：左边聊天、中间右边看文档，面板能流畅地展开收起，不是硬邦邦地蹦出来，而是顺滑地滑开，体验上更高级。
**第二，自动画的那些图**：架构图、数据库图都能**放大缩小、拖动、还能导出成图片**保存，看大图小图都清楚。我还做了个小优化，让页面**第一次打开快了一大截**（省了大概 2MB 的加载）。
**第三，文档排版**：生成的文字带格式、代码有高亮，读起来跟正经文档一样规整。
**第四，最让我得意的——生成进度像看快递物流**：哪怕你切到别的页面去干别的，它在后台照样跑，跑完了还会**弹个提示告诉你'好了'**，你完全不用守着屏幕干等。"

**【被追问 / 想秀深度】**
"三栏用 flex+width+opacity 配 GSAP 做动画；Mermaid 动态 import 省 2MB 首包、ssr:false 客户端渲染、支持 0.1×–10× 缩放和 SVG 导出；Markdown 用 react-markdown+remark-gfm+Shiki，Block 级 memoization；生成状态放在全局 Store，SSE reader 脱离 React 生命周期、用 Map 按会话管理、AbortController 支持取消；跨页面靠 GenerationWatcher + Toast 通知。"

**【一句话过渡】**
"界面这块就是这样。接下来是负责'看不见但很关键'的后台那一面，交给健皓。"

---

## 17–20 —— 团队协作 · 徐健皓（后端优化）

> 这四页是同一人连续内容。核心一句话：**让系统又快、又稳、又不丢东西。**

**【这页念这个】**
"大家好，我负责的是**用户看不见、但特别关键**的后台，做了四件让系统'更快更稳'的事——
**第一，让查东西变快**：原来数据一多，系统找一条记录得**从头到尾一页页翻**，慢。我给数据建了'目录页'，现在能**直接翻到那一页**，数据再多也快。
**第二，改文档不丢历史**：前面提过的'只增不删'，落地在我这边——每改一版都**新存一份、不覆盖老的**，等于给文档配了后悔药。
**第三，给系统装了个'体检仪'**：自动统计网页快不快、卡不卡，**不用我们手动一个个去记录**，哪里慢了一眼就看到。
**第四，给输入上'三道闸门'**：防止有人一次塞进来一篇超长的东西把系统搞乱——我在入口、中间、最里面各设了一道长度上限，层层把关，保证稳定。"

**【被追问 / 想秀深度】**
"索引：给 sessions/documents/messages 的外键和分享表建 B-tree 索引，从 O(n) 全表扫降到 O(log n)。append-only 落在 createDocument，新版本号 = 旧版 + 1。集成 Vercel Analytics，根布局挂 Analytics 组件零埋点采集 Web Vitals。输入三层漏斗：入口 Zod max(10000)，历史层 20 条 / 4000 字，Agent 层 prompt 10000 / context 20000。"

**【一句话过渡】**
"三个人的部分都讲完了，最后我来收个尾，说说我们最想让大家记住的几点。"

---

## 21 —— 总结

**【这页念这个】**
"最后用一分钟收尾。回到开头那个比方——**我们做的，就是一个能把'一句话需求'变成'整套软件设计图纸'的 AI 装修公司。** 我们最想让大家记住五件事：
**第一**，让 AI 自己判断'该动手了'并直接按按钮，而不是我们写死规则；
**第二**，生成全程在后台跑、跨页面实时通知，你不用守着；
**第三**，不是填表，是**聊天**把需求问清楚再自动开工；
**第四**，每一版都存档、永不覆盖，设计的演化全程可追溯；
**第五**，它还会参考你的个人偏好来生成，越用越懂你。
这就是 CycleMind。感谢各位老师，欢迎提问。"

**【一句话过渡】**
（结束，进入提问环节。被问到技术细节，翻最上面那张「术语对照速查表」。）

---

> **临场小贴士**
> - 任何一页被问住，先用类比答一遍，再说"技术上是这样实现的"补一句术语——顺序别反。
> - 全场记住一根主线：**装修公司 / 出全套设计图 / 多个工种同时干 / 改方案留旧图**。串不下去时回到这根线。
